<h1><%= t('.header') %></h1>

<table>
  <tr>
    <th><%= Test.human_attribute_name(:title) %></th>
    <th><%= Test.human_attribute_name(:level) %></th>
    <th><%= Test.human_attribute_name(:category_id) %></th>
    <th><%= Question.model_name.human(count: 9) %></th>
    <th colspan="3"><%= t('general.actions') %></th>
  </tr>
  
  <%= render @tests %>
</table>

<p>
  <%= link_to t('.new_test'), new_admin_test_path %>
</p>