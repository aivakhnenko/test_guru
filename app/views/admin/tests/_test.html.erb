<tr>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td><%= test.category.title %></td>
  <td><%= test.questions.count %></td>
  <td><%= link_to t('general.show'), admin_test_path(test) %></td>
  <td><%= link_to t('general.edit'), edit_admin_test_path(test) %></td>
  <td><%= link_to t('general.delete'), admin_test_path(test), method: :delete, data: { confirm: t('general.are_you_sure?') } %></td>
</tr>