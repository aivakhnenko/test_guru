<tr>
  <td><%= question.body %></td>
  <td><%= link_to t('general.show'), admin_question_path(question) %></td>
  <td><%= link_to t('general.edit'), edit_admin_question_path(question) %></td>
  <td><%= link_to t('general.delete'), admin_question_path(question), method: :delete, data: { confirm: t('general.are_you_sure?') } %></td>
</tr>