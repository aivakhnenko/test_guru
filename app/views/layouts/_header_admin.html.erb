<div class="nav nav-admin">
  <span class="nav-admin title">
    <%= link_to "#{t('general.project_name_short')} #{t('general.admin_panel')}", admin_path %> |
    <%= link_to t('.user_interface'), root_path %>
  </span>
  <div class="nav-admin user">
    <%= raw t('.welcome_message', email: "<b class=\"nav-admin user-email\">#{current_user.email}</b>") %> |
    <%= link_to t('general.logout'), destroy_user_session_path, method: :delete %>
  </div>
</div>
<%= flash_messages %>