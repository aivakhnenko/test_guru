<p class='nav'>
  <span class='title'>
    <%= link_to 'Test Guru &mdash; Online Tests'.html_safe, root_path %>
  </span>
  <span class='user'>
    <% if logged_in? %>
      Welcome, <b><%= current_user.name %></b> Guru |
      <%= link_to 'Logout', logout_path, method: :delete, data: { confirm: 'Are you sure?' } %>
    <% else %>
      <%= link_to 'Login', login_path %> |
      <%= link_to 'Sign up', signup_path %>
    <% end %>
  </span>
  <hr>
</p>